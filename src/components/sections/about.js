import React, { useEffect, useRef } from 'react';
import { StaticImage } from 'gatsby-plugin-image';
import styled from 'styled-components';
import { srConfig } from '@config';
import sr from '@utils/sr';
import { usePrefersReducedMotion } from '@hooks';

const StyledAboutSection = styled.section`
  max-width: 900px;

  .inner {
    display: grid;
    grid-template-columns: 3fr 2fr;
    grid-gap: 50px;

    @media (max-width: 768px) {
      display: block;
    }
  }
`;
const StyledText = styled.div`
  ul.skills-list {
    display: grid;
    grid-template-columns: repeat(2, minmax(140px, 200px));
    grid-gap: 0 10px;
    padding: 0;
    margin: 20px 0 0 0;
    overflow: hidden;
    list-style: none;

    li {
      position: relative;
      margin-bottom: 10px;
      padding-left: 20px;
      font-family: var(--font-mono);
      font-size: var(--fz-xs);

      &:before {
        content: '▹';
        position: absolute;
        left: 0;
        color: var(--green);
        font-size: var(--fz-sm);
        line-height: 12px;
      }
    }
  }
`;
const StyledPic = styled.div`
  position: relative;
  max-width: 300px;

  @media (max-width: 768px) {
    margin: 50px auto 0;
    width: 70%;
  }

  .wrapper {
    ${({ theme }) => theme.mixins.boxShadow};
    display: block;
    position: relative;
    width: 100%;
    border-radius: var(--border-radius);
    background-color: var(--green);

    &:hover,
    &:focus {
      outline: 0;
      transform: translate(-4px, -4px);

      &:after {
        transform: translate(8px, 8px);
      }

      .img {
        filter: none;
        mix-blend-mode: normal;
      }
    }

    .img {
      position: relative;
      border-radius: var(--border-radius);
      mix-blend-mode: multiply;
      filter: grayscale(100%) contrast(1);
      transition: var(--transition);
    }

    &:before,
    &:after {
      content: '';
      display: block;
      position: absolute;
      width: 100%;
      height: 100%;
      border-radius: var(--border-radius);
      transition: var(--transition);
    }

    &:before {
      top: 0;
      left: 0;
      background-color: var(--navy);
      mix-blend-mode: screen;
    }

    &:after {
      border: 2px solid var(--green);
      top: 14px;
      left: 14px;
      z-index: -1;
    }
  }
`;

const About = () => {
  const revealContainer = useRef(null);
  const prefersReducedMotion = usePrefersReducedMotion();

  useEffect(() => {
    if (prefersReducedMotion) {
      return;
    }

    sr.reveal(revealContainer.current, srConfig());
  }, []);

  const skills = [
    'JavaScript (ES6+)',
    'TypeScript',
    'React',
    'Node.js',
    'NestJS',
    'Express.js',
    'AWS',
    'GCP',
    'MongoDB',
    'PostgreSQL',
    'Firebase',
    'GitHub Actions',
  ];

  return (
    <StyledAboutSection id="about" ref={revealContainer}>
      <h2 className="numbered-heading">About Me</h2>

      <div className="inner">
        <StyledText>
          <div>
            <p>
              Hello! I'm Bilal, a Full-Stack Software Engineer with over 6 years of experience
              crafting web applications and cloud solutions. I enjoy bringing ideas to life through
              code, focusing on building reliable systems that make a real difference for users and
              businesses alike.
            </p>
            <p>
              I work across the entire development stack, with a particular interest in backend
              development, cloud integration, and efficient CI/CD workflows. I also enjoy creating
              responsive, intuitive interfaces using React and Tailwind that help users accomplish
              their goals quickly and easily.
            </p>
            <p>
              I've been fortunate to collaborate with talented people at companies like{' '}
              <a href="https://bytemage.io/">ByteMage</a>, where I helped develop an enterprise
              workforce management system, and <a href="https://aisight.ai/">AiSight.ai</a>, where
              we built data visualization tools that transformed complex information into actionable
              insights.
            </p>
            <p>
              Currently at <a href="https://case-elegance.co.uk/">Case Elegance</a>, I'm working on
              authentication flows, subscription systems, and RESTful APIs that connect with various
              services. Every day presents new puzzles to solve and opportunities to improve user
              experiences through thoughtful engineering.
            </p>
            <p>
              One of the most rewarding aspects of my work has been sharing knowledge with junior
              developers. There's something special about seeing others grow in their skills and
              confidence. I believe good code is as much about collaboration and communication as it
              is about technical implementation.
            </p>

            <p>Here are some technologies I work with regularly:</p>
          </div>
          <ul className="skills-list">
            {skills && skills.map((skill, i) => <li key={i}>{skill}</li>)}
          </ul>
        </StyledText>

        <StyledPic>
          <div className="wrapper">
            <StaticImage
              className="img"
              src="../../images/me.jpg"
              width={500}
              quality={95}
              formats={['AUTO', 'WEBP', 'AVIF']}
              alt="Headshot"
            />
          </div>
        </StyledPic>
      </div>
    </StyledAboutSection>
  );
};

export default About;
